import {
  StyleSheet,
  Text,
  View,
} from "react-native";
import { useRouter } from "expo-router";

const Home = () => {
  const router = useRouter();
  return (
    <View>
      
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({
});
